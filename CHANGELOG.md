# Changelog

Mudanças relevantes do projeto. Formato inspirado no [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/); a versão do firmware é a de `APP_VERSION_*` em `zephyr_app/include/app_types.h`. Toda mudança entra em **Não lançado** no mesmo commit do código.

## [Não lançado]

Revisão completa de 2026-09-18: análise do legacy e do port, migração para o NCS v3.3.0, correções críticas, testes de host e documentação. Nada foi testado na placa nem no nRF52840-DK.

### Adicionado

- **O ANT+ abre canais** (`src/rf/ant/ant_channel.c` e `include/rf/ant_channel.h`, `src/rf/ant/ant_sensors.c` e `.h`, mais a ligação do radar e do medidor de potência). Era 35 linhas: a pilha subia, a chave ANT+ entrava na rede e **nenhum canal era aberto**, de modo que nenhum sensor ANT+ podia ser encontrado — e a maioria dos equipamentos de ciclismo fala ANT+. Agora há o mecanismo: um canal escravo com número de dispositivo curinga, que acha o sensor que estiver perto, uma busca limitada a 30 s (um canal buscando para sempre mantém o rádio ligado e a bateria descendo atrás de um sensor que não existe) e o travamento no número de série depois da primeira mensagem, para seguir o sensor do ciclista e não o da bicicleta que passou. Quatro usos: frequência cardíaca, cadência, potência e radar. **Os parâmetros continuam fora**: tipo de dispositivo, período e frequência são dos perfis ANT+, sob licença que proíbe redistribuir, e vêm do Kconfig, onde são zero; a decodificação das páginas é função fraca definida em `src/rf/ant/sensor_pages.c`, que o `.gitignore` mantém fora. Um canal com tipo zero não abre e responde `-ENOTSUP`, que é como todo o ANT+ se comporta neste repositório: presente, ligado e calado. Quem tem sensor Bluetooth não precisa de nada disso. **Nada foi testado com sensor nenhum.**

- **O `$QRY` responde** (`src/model/qry.c` e `.h` com `test_qry` de 19 casos, `src/app/app_cmd.c`). Era um toco que devolvia `$QRY,0` e registrava "not answered yet". O `$QRY` **não existe no legacy** — é invenção do port —, então a semântica foi definida aqui: `$QRY,1` lista o que há no armazenamento (uma linha por arquivo com nome e tamanho, e `END` com a contagem), `$QRY,3,<nome>` apaga um arquivo, e **`$QRY,2` recusa de propósito**. Mandar arquivo pela serial em notificações de vinte bytes seria mais lento e sem nada do que o mcumgr sobre SMP já faz com deslocamento, soma de verificação e retomada; a resposta aponta para lá (`ERR,USESMP`). Apagar passa pela política de `model/file_policy.h`, que já existia para as transferências pelo ar: **uma atividade não pode ser apagada pela serial**, porque apagar é escrever e a regra já dizia que atividade sai e não entra. Quatro horas de pedal não vão embora porque alguém mandou uma frase. Os motivos vão em palavra (`NOTFOUND`, `FORBIDDEN`) e não em número, porque quem lê é uma pessoa num terminal ou um script.

- **A placa própria do projeto existe no firmware** (`zephyr_app/boards/gnss/gnssbike/`, alvo `gnssbike/nrf54lm20a/cpuapp`, e `tools/fw/board_check.py`). Até aqui só havia overlay do **nRF54LM20 DK**, que põe os periféricos da placa em pinos livres de um kit; agora há a placa: cada dispositivo que o aparelho carrega, no pino em que ele o carrega, com `board.yml`, `.dts`, pinctrl, Kconfig e defconfig. Escrever o devicetree achou três coisas que a alocação de pinos do [14](docs/14-hardware-placa-nova.md#alocação-de-pinos) não previa, todas do silício e nenhuma visível no papel:
  1. **`i2c30` e `uart30` são o mesmo bloco serial.** Cada bloco do nRF54LM20A tem um periférico; a alocação punha o I²C da energia e o console do lado. O console foi para o bloco 20 (P1.00 e P1.31).
  2. **O MCUboot é construído do mesmo arquivo de placa** e não tem driver de flash SPI: uma `fixed-partitions` na NOR externa a punha no mapa de flash de todas as imagens e deixava o bootloader ligando contra um dispositivo que nunca foi compilado. A tabela de partições e o disco foram para o overlay da aplicação.
  3. **O driver do GNSS inicializava antes do regulador que o alimenta** (prioridade 80 contra 86 do nPM1300). Isso só apareceu porque a placa declara `vcc-supply = <&npm1300_buck1>`, que o overlay do DK não tem; o Zephyr recusa o link e a prioridade do receptor passou para 90.
  O microSD saiu da definição: um arquivo de placa descreve **uma** placa, e a do produto não tem soquete (decisão do dono em 2026-09-20). O `board_check.py` confere o mapa a cada mudança — pino em dois lugares, SCL e SCK fora dos pinos de clock (tabela 79), pads do NFC e do cristal ocupados, pino além do tamanho da porta, dois periféricos no mesmo bloco serial e apelido que falta — e foi ele que mostrou que o RESET_N do GNSS estava planejado e não ligado. Sobram 32 pinos livres, 12 deles de clock. **Nada disso foi montado: não há esquemático nem unidade**, e o mapa ainda precisa ser conferido contra a pinagem do módulo BM20C quando ela chegar.

- **Notificações do celular na tela** (`src/model/notif_filter.c` e `.h` com `test_notif_filter` de 23 casos, `src/rf/ble_ancs_client.c` e `.h`, Kconfig do ANCS). Uma chamada, uma mensagem ou um lembrete de agenda aparecendo no guidão, para o ciclista decidir se vale tirar a mão do guidão. O transporte é o **Apple Notification Center Service** e o cliente é o que o nRF Connect SDK já traz (`bt_ancs_client`), como manda a regra de usar os subsistemas do SDK; nada dele foi reescrito. Vale para **iPhone**: o serviço é da Apple, e o Android não tem equivalente a que um aparelho simplesmente se conecte — precisaria de um app próprio, que este projeto não tem. O trabalho de verdade está no **filtro**, que é módulo à parte e testado: o celular entrega tudo o que tem, e quem vê tudo desliga o recurso antes do fim do primeiro pedal. As regras, em ordem: o que o celular marca como **pré-existente** é descartado (senão uma manhã de mensagens não lidas cai na tela na primeira pedalada), evento de remoção não mostra nada, **chamada recebida passa por cima de todas as outras regras**, o modo "só chamadas" serve para treino duro, o que o celular silenciou fica silencioso, categoria desligada não passa, identificador repetido não passa (mensagem editada chega de novo com o mesmo) e nada dentro de 20 s da anterior passa, a menos que o celular a marque como importante — um grupo faz trinta notificações por minuto. Achado ao escrever o teste: o identificador **zero** colidia com os espaços vazios do anel de repetidas, e a primeira mensagem de um pedal sumia. No nRF52840 o SMP, o pareamento e o ANCS levam a RAM a 90,0 %; no alvo de verdade, o nRF54LM20A, ficam em 74,8 %. Não testado com celular nenhum.

- **Treino estruturado e controle ERG do rolo** (`src/model/workout.c` e `.h`, `test_workout` com 32 casos, arquivos `.WKT` listados pelo armazenamento, comandos `APP_CMD_WORKOUT_SELECT`, `_START` e `_STOP`). "Dez minutos leve, depois quatro vezes cinco minutos a 280 W com três de intervalo, depois dez leve": qualquer outro ciclocomputador aceita isso e conta regressivamente, e este não aceitava — e o `ble_fec_client_set_target_power()` que já existia não era chamado por ninguém, de modo que o rolo era **assistido e nunca comandado**. O arquivo é texto, uma instrução por linha (`NAME`, `S`, `REPEAT`, `END`, `#`), no espírito dos arquivos de percurso do projeto; um passo termina por tempo, por distância ou **na tecla de volta**, e mira potência, batimento, cadência ou nada. As repetições são **achatadas** na carga — `REPEAT 4` sobre dois passos vira oito —, o que custa memória e paga uma contagem que nunca precisa perguntar onde está e um "passo 6 de 11" que quer dizer isso mesmo. Um arquivo que não cabe ou que tem uma linha errada é recusado **inteiro**: meio plano é pior que nenhum, porque o ciclista pedalaria o treino errado sem saber. No ERG o rolo recebe o meio da faixa, e só dentro de casa: pedir watts a uma máquina que não está ali não faz sentido. Não testado com rolo nenhum.

- **Alertas configuráveis** (`src/model/alerts.c` e `.h`, `test_alerts` com 22 casos, comando `APP_CMD_SET_ALERT`, guardados nas configurações). Doze: frequência cardíaca, potência, velocidade e cadência, cada uma alta e baixa; distância a cada tanto; tempo, beber e comer a cada tantos minutos **em movimento**, de modo que uma hora no café não aproxima o próximo lembrete. O alerta em si é uma comparação; o que o módulo resolve é **não encher o saco**: quem fica exatamente no limite é avisado uma vez, não a cada época, e só volta a ser avisado depois de cair de volta para dentro por uma margem (3 bpm, 15 W, 1,0 km/h, 3 rpm — absolutas, porque 5 % de 40 bpm é 2 e 5 % de 1.000 W é 50) **e** depois de um minuto. Um alerta "abaixo de" só fala depois que a grandeza apareceu, senão uma cinta desconectada, que lê zero, diria que o coração parou. Um salto que passa dois marcos de distância de uma vez avisa uma vez. As configurações ganharam a versão 4: um bloco salvo pela anterior é descartado pela checagem de versão, como já era o desenho.

- **Cliente de medidor de potência**, por Bluetooth e por ANT+ (`src/model/cps_parse.c` e `.h` com `test_cps_parse` de 23 casos, `src/rf/ble_cps_client.c` e `.h`, `src/rf/ant/power_ant.c` e `include/rf/power_ant.h`, ligação no gerenciador BLE, no serviço de rádio e no modelo). Era a maior lacuna do port: havia cliente de frequência cardíaca, de cadência, de rolo e de radar, mas **nenhum** de potência, e ao ar livre a potência na tela era só a **estimativa** da fórmula do legacy sobre velocidade, inclinação e peso. Agora quem tem medidor vê o que o medidor diz, e `ride_power_w()` no serviço do modelo escolhe: medidor fresco primeiro, depois o rolo, depois a estimativa. A leitura da característica (0x2A63) fica num módulo puro porque a caminhada pelos campos opcionais é onde moram os erros — a armadilha é que **o relógio da roda bate a 1/2048 s** enquanto o da pedivela e os dois do serviço de cadência batem a 1/1024 s, e quem assume um só reporta o dobro ou a metade da velocidade. Vêm junto o balanço entre as pernas, o torque acumulado, a energia e o pedido de zero offset. O lado ANT+ segue o mesmo arranjo do radar: a tubulação está aqui e o perfil Bicycle Power fica fora do repositório (`CONFIG_GNSS_ANT_POWER_DEV_TYPE` é zero, `src/rf/ant/power_pages.c` está no `.gitignore`), porque a licença do perfil proíbe redistribuir e o repositório é público. A ordem e as larguras dos campos vêm da especificação como este port a entende: **nenhum medidor foi à bancada**, e a primeira sessão com um deve confirmar a caminhada pela cadência, onde um deslocamento aparece na hora.

- **Potência normalizada, fator de intensidade, estresse de treino e índice de variabilidade** (`src/model/power_metrics.c` e `.h`, `test_power_metrics` com 19 casos, faixa nova na tela do rolo). São os três números que um ciclista com medidor de potência olha no fim do pedal, e que faltavam: duas horas firmes a 200 W e duas horas alternando 100 e 300 W têm a mesma média e custam coisas muito diferentes ao corpo. O método é o de Andrew Coggan, o mesmo de todo head unit e de todo site de treino: média móvel dos últimos 30 s de potência, cada valor elevado à quarta, média disso, raiz quarta. Daí saem IF (`NP/FTP`), TSS (`t × NP × IF / (FTP × 3600) × 100`, de modo que uma hora exata no limiar dá 100) e VI (`NP/média`, que diz o quanto o pedal foi irregular). O modelo alimenta **um valor por segundo de tempo em movimento** — o cronômetro da atividade já tira as pausas, então parar no semáforo não enche a janela de zeros. Sem FTP configurado, IF e TSS ficam em zero em vez de inventar número. Uma potência acima de 2.500 W entra como zero, não como descarte, porque descartar deslocaria a janela. Não testado com medidor nenhum.

### Corrigido

- **Três recursos prontos que o ciclista não alcançava.** O treino estruturado, a escolha de um treino e os doze alertas estavam no modelo, com teste, e **sem caminho até o dedo**: a tela de treino existia na tabela e nada navegava até ela, os `.WKT` eram listados e não havia onde escolher, e o `APP_CMD_SET_ALERT` funcionava sem tela que o mandasse. Agora o menu tem **Treino**, que abre a sessão carregada ou a lista para escolher (com *Descarregar* enquanto há uma), e os ajustes têm **Alertas** e **Lembretes**. Ao desenhar apareceu um defeito: o `ui_list_create()` desenha **doze linhas e não rola**, então Voltar mais doze alertas deixaria o lembrete de comer inalcançável — daí os dois grupos, que é a divisão que um ciclista faria de qualquer jeito (os oito que vigiam um número e os quatro que tocam de tempos em tempos). Três telas novas no renderizador (`40_treinos`, `41_alertas`, `42_lembretes`).

- **A potência do medidor não chegava na tela.** O `model_ui.c:246` mostrava `att.pwr`, que é a **estimativa** da fórmula do legacy: com um medidor ligado, o campo de potência continuava exibindo o palpite. A cadência era lida só do sensor de cadência separado, de modo que um medidor de pedivela — que conta o pedal — não aparecia. A escolha entre medidor, rolo e estimativa saiu do serviço e virou `model_ride_power_w()` e `model_ride_cadence_rpm()` no modelo, e **a tela e a gravação passam pela mesma**, de modo que não podem discordar sobre o que o pedal estava fazendo.
- **O treino estruturado não tinha tela nenhuma.** O ciclista não via passo, contagem, alvo nem "passo 6 de 11". Agora há `UI_SCREEN_WORKOUT` (`ui_scr_modes.c`): nome da sessão, passo e total, rótulo do passo, quanto falta (tempo, metros ou "tecla"), alvo, valor atual **na cor da resposta** — verde dentro da faixa, vermelho fora, porque na guidão se lê uma cor antes de um número — e uma barra com a faixa alvo e a marca de onde o ciclista está. Duas telas novas no renderizador (`38_treino`, `39_treino_sem_arquivo`). Ao desenhar apareceram dois defeitos do primeiro traçado: o campo corta em 6 caracteres e vira `---`, e a fonte grande de valor só tem dígitos, de modo que a letra da unidade saía como caixa; a unidade foi para o canto do campo e o valor virou número puro.

### Mudado

- **O nRF52840 deixou de ser o alvo padrão** (decisão do dono, 2026-09-22): `tools/fw/fw.sh` compila para `nrf54lm20dk/nrf54lm20a/cpuapp` sem `BOARD`. O nRF52840 DK ainda compila com `BOARD=nrf52840dk/nrf52840`, mas não é mais verificado nem reportado.

- Quatro defeitos no coração do modelo, achados ao escrever o primeiro teste do `attitude.c` (`src/model/attitude.c`, `src/model/crash_recovery.c`, com `test_attitude` de 18 casos e `test_crash_recovery` de 8).
  1. **Segundos ativos pela metade.** O contador estava atrás de `(now - last_update) >= 1000`, medido no relógio do sistema no instante em que a thread chegava à época, e o `last_update` só andava quando o teste passava. Uma época chegando 998 ms depois da anterior não contava **e** não movia a referência, então a seguinte passava com dois períodos de intervalo e ainda contava **um**: alguns milissegundos de jitter de escalonamento viravam um conta-perde-conta-perde e cortavam o número ao meio. A velocidade média da página 1 é `dist × 3,6 / nbsec_act`, então o denominador pela metade mostrava o dobro da velocidade. Passa a contar uma vez por época pedalada, que é o que o `legacy/source/model/Attitude.cpp:509` faz.
  2. **Pedal recuperado voltava com um segundo.** O `restore_from_crash()` punha `saved.nbsec_act` na estrutura publicada mas deixava o contador por trás dela em zero, e a primeira época pedalada depois do restauro escrevia 1 por cima dos segundos do pedal inteiro.
  3. **Pedal recuperado voltava com desnível zero.** O bloco do nível do mar marcava `is_altitude_initialized = false` e **depois** restaurava; o `update_climb()` da mesma época via a marca e zerava o desnível recém-restaurado. O legacy faz na ordem certa — chama o `filterElevation()`, que é quem zera, **antes** de restaurar (`Attitude.cpp:390-416`).
  4. **O pedal restaurado era o próprio pedal em curso.** O bloco da recuperação fica em RAM não inicializada e o pedal em andamento o reescreve a cada 15 m, a partir dos primeiros 25 m de movimento; o restauro, porém, só acontece quando a referência do nível do mar aparece, depois de mais de quinze posições — um ou dois minutos adentro. Nessa altura o pedal novo já tinha sobrescrito o antigo uma dúzia de vezes, e o que voltava era o pedal que já estava rodando: a recuperação inteira não servia para nada. **O legacy tem o mesmo buraco** (`Attitude.cpp:393-416` lê o bloco vivo naquele momento tardio); aqui o bloco passa a ser copiado no boot, dentro do `crash_recovery_init()`, antes que qualquer coisa possa escrevê-lo. Junto foi o caso da partida nova: o `memset` do `init()` deixava `crc = 0`, e o CRC-8 de um bloco de zeros é zero, então um aparelho ligando pela primeira vez anunciava ao ciclista que um pedal tinha sido recuperado e restaurava um pedal de nada.

- Radar traseiro por ANT+ que nunca abriria o canal (`svc/radio/radio_svc.c`). O serviço de rádio sobe a pilha ANT e inicia o radar por BLE, mas ninguém chamava `radar_ant_start()`: a ligação faltava. Neste repositório a chamada responde `-ENOTSUP`, porque os parâmetros do canal do perfil ANT+ Bike Radar não estão aqui (`CONFIG_GNSS_ANT_RADAR_DEV_TYPE` é zero: o perfil está sob licença que proíbe redistribuir e o repositório é público), mas é o gancho que faltava para quem tem o perfil. Achado por uma varredura de módulos sem nenhum chamador em todo o firmware.

- `src/model/kalman.c` e `.h`, 139 linhas de filtro de Kalman escalar e de posição sem nenhum cliente: o único era o `locator.c`, que saiu. O `kalman_altitude.c`, que é o filtro de 3 estados portado do `Attitude.cpp`, não depende dele. O binário não mudou de tamanho, o que confirma que o linker já o descartava.

### Testado

- `test_rr_zone`, 14 casos, contra `legacy/source/model/RRZone.cpp`: a amostra sem relógio, a primeira amostra que só acerta o relógio, os 20 elementos do buffer, o RMSSD com o **denominador do legacy** (a raiz de `soma_dos_quadrados / 20`, o tamanho do buffer, e não as 19 diferenças — se virar 19 o valor sai 40 em vez de 38,99 e o teste cai), cada um dos seis limites de zona, o fato de a zona ser a do batimento da **última** amostra e a média por zona. Fica registrado que o `hrm_info.timestamp = 0` do legacy (`RRZone.cpp:79`) **não** foi portado de propósito: lá o `Model.cpp:378` entrega a mesma struct global a cada volta do laço e o zero é o que impede contar a mesma amostra várias vezes; aqui o chamador monta uma struct nova por notificação. Cinco mutações derrubam o teste — inclusive a que tira a guarda do relógio, que só cai com a amostra zerada chegando **depois** de o sensor já estar rodando.

- Máquina de modos sem guarda nenhuma, e apagando os números do ciclista (`src/model/mode_fsm.c` e `.h`, `svc/model/model_svc.c`, `test_mode_fsm` com 22 casos). Ela morava dentro do serviço, não tinha teste, e o `boucle__change_mode()` do legacy que ela copiava (`legacy/source/model/Boucle.cpp:101-142`) não guarda nada: dava para entrar em qualquer modo a qualquer momento. Agora é um módulo próprio, com as duas famílias (ao ar livre CRS, PRC e DBG; dentro de casa FEC e Zwift) como estados pais do SMF, e três regras — cada uma era um jeito de perder um pedal:
  - **entrar em PRC sem percurso carregado** deixava uma tela de navegação sem navegação; a troca passa a ser recusada com aviso na tela, e um percurso que sai debaixo de quem o segue devolve o modo para CRS;
  - **trocar de família gravando** misturava dado de rolo no arquivo de um pedal de rua; dentro da mesma família a troca continua livre;
  - **entrar em FEC chamava `power_zone_reset()` e `suffer_score_reset()`**, e quem punha a bicicleta no rolo perdia as zonas de potência e o suffer score do pedal inteiro. Eram as **duas únicas** chamadas de reset dessas estruturas no firmware: fora desse caminho as zonas nunca zeravam, e o `rr_zone_reset()` não é chamado em lugar nenhum. Os números da sessão são da atividade, não do modo, e a máquina não encosta mais neles. As cinco mutações testadas (tirar cada guarda, mudar a família do FEC, tirar a saída do PRC) derrubam o teste.

- Duas cópias do modelo do pedal que ninguém lia, com fórmulas próprias e divergentes (`src/model/locator.c` e `src/model/parcours.c`). O `locator.c` — 379 linhas — mantinha um histórico de posições, um total de distância, um total de subida, uma rampa, uma velocidade e três filtros de Kalman próprios, e **nenhuma** dessas saídas era lida em lugar nenhum: quem calcula distância é o `distance.c` e quem calcula rampa é a fusão do `kalman_altitude.c`. Ele também não era um port do `Locator` do legacy, que é o árbitro de fonte de posição (extraído para `model/loc_arbiter.c`): era código inventado que tomou o nome. E tinha um defeito real — a janela da rampa somava `dist_from_prev` das idades 1 a N, quando o trecho entre a idade N e a atual são as idades 0 a N−1, de modo que o denominador da rampa era sistematicamente o trecho errado, deslocado de um segmento nas duas pontas. O módulo saiu inteiro; a única coisa dele que era usada, a validação da posição, virou `latlon_is_valid()` no `vecteur.c`, com a regra do legacy (`legacy/source/routes/Points.cpp:37-42`) que o `point_is_valid()` já usava. O `parcours.c` media o percurso com haversine e raio 6.371.000 m, enquanto a distância pedalada usa a equiretangular do legacy com 6.371.008 m: num percurso de 100 km o "quanto falta" e o "quanto andei" discordavam em até 500 m. Agora há uma implementação só. nRF52840 −884 B de FLASH e −1.920 B de RAM; nRF54LM20 −880 B e −1.936 B ([06](docs/06-algoritmos.md#distância)).

- Escolha da fonte de posição fora da regra do legacy, e um módulo morto ao lado (`src/model/loc_arbiter.c` e `.h`, `test_loc_arbiter` com 14 casos; saiu o `src/model/loc_source.c`, 299 linhas que ninguém chamava). A regra do `legacy/source/model/Locator.cpp:111-134` tem duas sutilezas que um "pega a mais nova" perde: uma fonte **recente mas não nova** não devolve nada, em vez de cair para a próxima — é isso que impede o receptor de enfiar uma posição entre dois quadros de um passeio simulado e fazer a bicicleta pular —, e o telefone é recusado enquanto o receptor tem fix, por mais velho que ele seja. A janela do simulado estava em 6000 ms no port, contra os 2000 ms do legacy, porque compartilhava por engano a constante `POS_MAX_AGE_MS`, que só serve para dizer se a barra de status mostra fix.

- Duas cópias do modelo do pedal que ninguém lia, com fórmulas próprias e divergentes (`src/model/locator.c` e `src/model/parcours.c`). O `locator.c` — 379 linhas — mantinha um histórico de posições, um total de distância, um total de subida, uma rampa, uma velocidade e três filtros de Kalman próprios, e **nenhuma** dessas saídas era lida em lugar nenhum: quem calcula distância é o `distance.c` e quem calcula rampa é a fusão do `kalman_altitude.c`. Ele também não era um port do `Locator` do legacy, que é o árbitro de fonte de posição (já extraído para `model/loc_arbiter.c`): era código inventado que tomou o nome. E tinha um defeito real — a janela da rampa somava `dist_from_prev` das idades 1 a N, quando o trecho entre a idade N e a atual são as idades 0 a N−1, de modo que o denominador da rampa era sistematicamente o trecho errado, deslocado de um segmento nas duas pontas. O módulo saiu inteiro; a única coisa dele que era usada, a validação da posição, virou `latlon_is_valid()` no `vecteur.c`, com a regra do legacy (`legacy/source/routes/Points.cpp:37-42`) que o `point_is_valid()` já usava. O `parcours.c` media o percurso com haversine e raio 6.371.000 m, enquanto a distância pedalada usa a equiretangular do legacy com 6.371.008 m: num percurso de 100 km o "quanto falta" e o "quanto andei" discordavam em até 500 m. Agora há uma implementação só. nRF52840 −884 B de FLASH e −1.920 B de RAM; nRF54LM20 −880 B e −1.936 B ([06](docs/06-algoritmos.md#distância)).

- Números de teste e de tela errados na documentação, achados numa revalidação do zero em 2026-09-21. O `CLAUDE.md` dizia **367 casos** de teste de host quando a contagem real, antes desta rodada de trabalho, era **349**: dezoito a mais, de antes desta sessão, que foram sendo somados a cada item novo até virarem 473. O número medido com a pasta de build apagada é **455 casos em 39 conjuntos**. Do mesmo modo, o título de `docs/telas/README.md` vinha contando telas a mais: são **35 telas em 39 quadros**, e duas seções (a atualização e o perfil do percurso) não estavam no índice. `docs/12` dizia 12 casos em `test_liste_points`, que tem 15.
- Segundo ativo contado a partir de 2 km/h, e não dos 7 km/h do legacy (`attitude.c`, `MOVING_SPEED_THRESHOLD`). `legacy/source/model/Attitude.cpp:509` é `if (loc_.speed > 7.f) att.nbsec_act++;`: com 2 km/h o port contava caminhada e giro solto, e a média da página 1 do CRS, que é `dist × 3,6 / nbsec_act`, saía abaixo da que o legacy mostra no mesmo passeio. A pausa automática de `model/activity.c` tem limiar próprio, muito mais baixo, porque só precisa distinguir bicicleta parada de bicicleta andando ([06](docs/06-algoritmos.md#cronômetro-pausa-automática-e-voltas)).
- Percursos que nunca abriam (`src/model/parcours.c`, `svc/storage/storage_svc.c`, `svc/model/model_svc.c`, `test_parcours`), quatro defeitos:
  - **formato errado**: o carregador esperava `lat;lon;alt` com ponto e vírgula, e os arquivos do legacy são `lat lon [alt]` separados por espaço (`legacy/source/parsers/file_parser.cpp:79-123`); nenhuma linha dos dois percursos reais de `tools/TDD/DB` seria aceita. As linhas de metadados (`<...>`) são puladas e um ponto sem altitude entra com zero, como no legacy.
  - **o CRLF terminava o arquivo**: a leitura, feita byte a byte, tratava a linha vazia do fim de linha do legacy como fim do arquivo e parava no primeiro ponto. Agora a leitura é em blocos, com as linhas montadas no código, e a última linha sem quebra também conta.
  - **`.PAR` não era aceito**: só a extensão `.CRS` entrava, e os arquivos do legacy são `.PAR`. Qualquer um dos dois abre; a lista do cartão passou a guardar o nome inteiro do arquivo, que é o que abre.
  - **fora do percurso não havia volta**: o `parcours_update()` só rodava no estado ativo, então ao passar de 50 m da linha o estado ia para `OFF_ROUTE` e nunca mais era atualizado. O legacy chama `updatePosAuParcours()` toda época enquanto há percurso (`BoucleCRS.cpp:104-111`).
  - E o percurso escolhido na tela agora é aberto de verdade: o modelo trata `APP_CMD_ROUTE_SELECT` abrindo o arquivo e, em PRC, começando a navegação. Um percurso maior que 500 pontos é dividido pela metade enquanto carrega, como os segmentos, em vez de ser cortado no meio. Não testado com cartão.

- Rádio BLE que nunca acharia nem falaria com nada (`src/rf/ble/ble_manager.c`, `ble_hrs_client.c`, `ble_bsc_client.c`, `ble_fec_client.c`, `svc/radio/radio_svc.c`), três defeitos:
  - **nada começava**: o `ble_manager_init()` só registrava callbacks; ninguém chamava o anúncio nem a varredura, então nenhum sensor era encontrado e nenhum telefone conseguia conectar (inclusive para a atualização por BLE). O serviço de rádio agora inicia os dois depois de subir a pilha.
  - **a inscrição derrubaria o aparelho**: `bt_gatt_subscribe()` com `ccc_handle = 0` e `CONFIG_BT_GATT_AUTO_DISCOVER_CCC=y` exige `disc_params` e `end_handle`, e sem eles o host do Zephyr 4.3 escreve em ponteiro nulo assim que o primeiro sensor responder. Os três clientes passaram a guardar o fim do serviço na descoberta e a dar ao host onde procurar o descritor.
  - **o pool de conexões esgotava**: o `bt_conn_le_create()` devolve uma referência que o chamador precisa soltar; o callback de conexão já guarda a sua. Sem o `bt_conn_unref()`, quatro tentativas acabavam com as conexões do controlador. Nada disso foi testado em placa (não há sensor nem rádio na bancada).

- Segmentos que nunca funcionariam, por cinco defeitos encadeados (`liste_points.c`, `segment.c`, `model_svc.c`, `app_types.h`, com `test_segment` e `test_liste_points`):
  - **os pontos não cabiam**: `liste_points_t` guardava 20 pontos num array interno, e o `segment.c` pedia 500 — a capacidade era cortada para 20 e um segmento maior ficava só com os **últimos** 20 pontos, ou seja, o "primeiro ponto" que a ativação, o alocador, o término e o progresso usam ficava no meio do percurso (os arquivos reais de `tools/TDD/DB` vão a 1.291 pontos). Agora uma lista pode morar num array de quem a cria (`liste_init_static()`), cada segmento carregado toma um slot de um pool de 3 × 256 pontos e um arquivo maior é dividido pela metade enquanto carrega (`liste_decimate()`), preservando o primeiro ponto, o último e a forma do percurso.
  - **o histórico congelava**: o `liste_add_iso()` cortava a lista baixando o contador, o que descarta o ponto recém-inserido em vez do mais antigo. Depois de `HISTO_POINT_SIZE` épocas a posição "atual" parava de andar, e com ela a ativação, o término e a posição relativa. Agora a janela anda como no `ajouteFinIso()` do legacy (`push_front` e `pop_back`), e `HISTO_POINT_SIZE` voltou aos 20 de `legacy/source/parameters.h:21` (estava 15).
  - **o alocador não tinha quem o chamasse**: nenhum segmento abria o arquivo. O serviço do modelo agora roda `segment_run_allocator()` uma vez por época, antes do `segment_update()`, como o `BoucleCRS.cpp:121` do legacy.
  - **descarregar não limpava tudo**: `num_points`, `total_time` e `total_elev` continuavam com os valores do arquivo depois de o segmento sair da memória.
  - **vizinhos e distância só viam o que estava carregado**: `segment_get_nearby()` e `segment_get_nearest_distance()` agora usam a posição codificada no nome do arquivo, como o alocador do legacy (`sd_functions.cpp:547-556`), então valem para o cartão inteiro.
  - Constantes acertadas com `legacy/source/routes/Segment.h`: `SEG_ALLOC_DIST` de 3.000 para 300 m, `SEG_MARGE_DESACT` de 1,5 para 2,0 e `SEG_DEACTIVATE_DIST` de 100 para 75 m. A troca do estado por segmento (50 × ~400 B) pelo pool deu RAM −6.848 B no nRF52840 DK. Não testado com cartão nem na placa.
- Listas de pontos invertidas (`liste_points.c`, `test_liste_points`): o `liste_add_back()` inseria no **início** e o `liste_add_front()` no fim, o contrário dos nomes. Um segmento lido do arquivo ficava de trás para frente, e o "primeiro ponto" que o alocador e a ativação usam era o último do segmento; o histórico do ciclista tinha a posição atual no fim, e não no índice 0, onde o teste de ativação a procura. Agora o `add_back` mantém a ordem do arquivo e o `add_front` põe o ponto novo no índice 0, como o `ajouteIso()` do legacy.
- Log da atividade no formato do legacy (`sd_logger.c`, `test_sd_logger`): arquivo `@<data>.txt` na raiz do cartão, com o dia sem zero à esquerda como o legacy escreve (`@50925.txt` é 5/9/25), sem linha de cabeçalho, com os 19 campos separados por `;` e terminados em CRLF — posição, altitude, segundos do dia, potência, batimentos, cadência, os dois ângulos do filtro, altitude do barômetro e a correção, subida, altitude filtrada, altitude do GPS, velocidade vertical e as quatro rugosidades. O port gravava um CSV próprio de 13 colunas em `/SD:/logs/`, e a altitude filtrada recebia a do GPS. O ponto publicado pelo modelo passou a levar os campos que faltavam, com a rugosidade do barômetro calculada como no legacy (desvio médio absoluto das dez últimas pressões).
- Recuperação de falha (FDIR) que nunca restaurava nada (`crash_recovery.c`, `attitude.c`, `test_crash_recovery`): o CRC-8 era calculado sobre `sizeof(saved_data_t) - 1`, que inclui o próprio campo de CRC por causa do enchimento da estrutura, então a verificação nunca correspondia ao que protegia; o `crash_recovery_has_data()` exigia uma falha registrada, e um reset pelo watchdog ou por bateria vazia não deixa nenhuma; e a restauração rodava no `attitude_init()`, antes de o aparelho saber a data, em vez de acontecer ao achar a referência do nível do mar e só com o bloco do mesmo dia, como no legacy. O bloco passou a levar também o indicador de recorde, a limpeza marca 0xFF no CRC (o CRC-8 de zeros é zero) e o aparelho avisa "FDIR / Atitude restaurada" na tela. A causa do reset é lida uma vez e guardada: a leitura do `hwinfo` limpa o registrador, e a segunda chamada devolvia "desconhecida".
- Filtro de altitude de 3 estados parado por três defeitos (`udmatrix.c`, `kalman_altitude.c`, `attitude.c`), com `test_kalman_altitude` e `test_udmatrix`: o `udmat_add()` chamava `udmat_init()` no destino, que zera os dados, e a predição faz `P⁻ = P⁻ + Q` com o destino igual à entrada, então a covariância prevista virava só `Q` e o ganho ficava em 3e-5 (a elevação andava 0,45 m em 120 s de subida de 38 m); o `udmat_ones()` gerava identidade, e o P0 do legacy tem 900 em todos os nove elementos; e o `udmat_bound()` comparava o valor com sinal, transformando toda covariância negativa em +1e-15, o que impedia estimar o offset de montagem do acelerômetro (α0). A fusão também passou a rodar uma vez por época, com a média das dez últimas pressões (`FILTRE_NB` do legacy), em vez de a cada amostra do barômetro com a pressão instantânea.
- Distância fiel ao legacy: a fórmula equiretangular de `libraries/utils/utils.h:36-48`, com o raio de 6.371.008 m, no lugar da haversine com 6.371.000 m (`vecteur.c`), e o `locator_calc_distance()` chamando a mesma função, sem duas implementações. O acúmulo passou para `src/model/distance.c` com a regra de `Attitude::computeDistance`: posições brutas, sem a porta de 2 km/h que o port tinha, descarte dos primeiros 25 m (a posição anterior à primeira é (0, 0)) e instantâneo a cada 15 m, que agora é quem salva o estado da recuperação de falha, em vez de uma vez por segundo. `test_distance` (9 casos) e `test_vecteur` contra a transcrição do legacy; mutação 14 de 15.
- Potência estimada fiel ao legacy (`src/model/power_estimate.c`, `test_power_estimate`): `P = 1,025·[9,81·W·v_z + 0,004·9,81·W·v + 0,204·v³]` com o peso só do ciclista (padrão 79 kg, o `USER_WEIGHT` do legacy, no lugar de 75) e a velocidade da posição anterior, como o legacy faz ao chamar `computePower()` antes de atualizar `m_speed_ms`. A fórmula própria do port dava 88 W a 30 km/h no plano contra os 147 W do legacy. A potência agora pode ser negativa na descida (`int16_t`, como o `SAtt.pwr`), saturando em vez de dar a volta, e o filtro de altitude passou a receber a mesma velocidade atrasada de uma época.
- Pinos do I2C no alvo nRF54LM20 DK, contra duas regras da ficha do nRF54LM20A (4539_001 v1.0): o SDA dos sensores estava em P1.02, que é o NFC2 e sai do reset como pino de antena, com a função de GPIO desligada (tabela 78 e registrador `PADCONFIG` do NFCT, que vale 1 no reset; o Zephyr só libera os dois com `nfct-pins-as-gpios` no nó `uicr`), e o SCL do I2C de energia estava em P1.12, que não é pino de clock, exigidos para o SCL do TWIM e o SCK do SPIM (tabela 79). Agora o SDA dos sensores fica em P1.29, vizinho do SCL P1.03 no encapsulamento, e o SCL de energia em P1.14; P1.01 e P1.02 ficam livres para a antena NFC. As amostras da Nordic para o nPM1300 EK usam P1.11 e P1.12. Não testado no DK.
- Zonas de potência e suffer score alimentados como o legacy: as zonas de potência só em FEC, com a potência do rolo a cada dado, qualquer que seja (`BoucleFEC.cpp:75`), e o score a cada 1 s, em todos os modos, com a FC do momento, 0 com a cinta perdida (`Model.cpp:377`). Antes, os dois eram alimentados por posição e só com valor acima de zero, e a pausa ia para a zona seguinte.
- `docs/18` dizia que a suavização do LVGL seria desligada: no LVGL 9.5 ela não desliga para as primitivas (`lv_display_set_antialiasing()` só alcança camadas e imagens), e o driver da tela passa a quantizar cada pixel para as cores do painel, com a regra no documento. As tabelas das telas seguem agora o legacy: Alt ao lado do próximo segmento na página 1, os campos do legacy na página 2 e no PRC, a lista de percursos no Modo PRC, o tempo decorrido no FEC e a hora na DBG; as diferenças que ficam estão listadas no documento e em `docs/06` (formatação dos números).
- Especificação da placa nova (`docs/14`, com `docs/13`, `docs/15`, `docs/16` e `docs/18`), pela segunda passagem da validação: o resistor do VSET1 do nPM1300 não escolhe 3,0 V (tabela 18), então o 3,0 V sai do BUCK2 (150 kΩ) e o 1,8 V do GNSS, do BUCK1 (47 kΩ), travado no devicetree porque o V_IO do MAX-M10N-10B tem máximo absoluto de 1,98 V; o BM20C mede 10,0 × 16,2 mm, e não 14,8 mm, e a área da antena tem regras próprias; o RF_IN do GNSS aguenta +15 dBm fora da banda, então o BLE não o danifica e o teste passa a ser de bloqueio; o MAX-M10N-10B gasta 13,7 mW em LEAP pela ficha do 10B, e a autonomia estimada passa de cerca de 320 h para cerca de 310 h; a tabela 6 do AEM10900 dá 65,5 mA de entrada com 6,8 µH, abaixo do pico do painel, e o indutor passa a 4,7 µH.
- `BUILD_DIR` relativo no `fw.sh` e no `build.bat` era resolvido a partir do `zephyr_app`, onde o build roda: o `BUILD_DIR=zephyr_app/build_54` da documentação criava `zephyr_app/zephyr_app/build_54`. Agora ele vale a partir da pasta de quem chama; o binário não muda.
- Estouro de pilha na thread `main_loop`: a cadeia do Kalman de altitude usa ~2.200 B e a pilha tinha 2.048 B; agora 4.096 B (`src/main.c`). A thread `display` passou de 1.024 para 2.048 B.
- O GPS e todo o modelo rodavam dentro da ISR da UARTE1, uma vez por sentença NMEA: a ISR agora só enfileira bytes num `ring_buf` e `hal_uart_process()` monta as linhas na `main_loop` (`src/hal/hal_uart.c`, `src/drivers/gps/gps_mgmt.c`).
- O callback de fix disparava 5 a 7 vezes por segundo com o mesmo ponto: agora uma vez por época, no RMC válido, e um RMC `V` encerra o fix na hora.
- Linhas NMEA sem validação de checksum no caminho usado pelo `gps_mgmt`.
- Parser NMEA: o caminho caractere a caractere nunca reconhecia uma sentença; milissegundos lidos errado (`.200` virava 2000 ms); coordenadas perdiam precisão ao virar `float` antes da divisão; a posição válida nunca voltava a falso (`src/drivers/gps/nmea_parser.c`).
- `sd_logger_add_entry()` escrevia além do buffer quando o cartão não estava disponível, corrompendo a memória depois de ~90 m de atividade (`src/model/sd_logger.c`).
- Botões lidos com inversão dupla: em repouso pareciam pressionados e, 1 s depois do boot, um `LONG_CENTER` parava e gravava a atividade (`src/hal/hal_gpio.c`).
- Reset e standby do GPS com polaridade invertida no overlay (o módulo ficaria em reset e em standby).
- Reset do FXOS8700 flutuando durante a inicialização do driver e com semântica invertida: `reset-gpios` ativo alto no nó e `imu_reset` corrigido.
- Nós do nRF52840-DK nos pinos da placa: `qspi` e `mx25r64` (CS do LCD), `spi3` (NeoPixel, FIX e standby do GPS) e `pwm0` (botão central) desligados; `uart0` sem RTS/CTS (pinos do GPS); `uart1` sem o pull-up herdado no TX.
- Erro fatal travava o aparelho: `CONFIG_RESET_ON_FATAL_ERROR=y`.
- Causa do reset sempre "desconhecida" no NCS v3.3.0: o Zephyr 4.3.99 trocou `CONFIG_SOC_SERIES_NRF52X` por `CONFIG_SOC_SERIES_NRF52` (`src/model/crash_recovery.c`).
- Aparência BLE 1157 (sensor de velocidade e cadência) trocada por 1153 (Cycling Computer).
- Scripts `.bat` apontavam para o NCS v3.1.0 e o toolchain `b8b84efebd`, que não existem mais, para caminhos de uma pasta antiga, e definiam `TOOLCHAIN_ROOT`, que quebra o CMake do Zephyr; o `build.bat` rodava o `west` no drive `C:` com o projeto em `F:`.
- Corrida entre threads no modelo: a thread `sensor` repetia a cada 1 s as leituras I2C que o `boucle_process()` já faz e escrevia os caches dos drivers lidos pela `main_loop`, e a `display` lia o modelo sem trava. A `sensor` saiu, a `main_loop` é a única escritora do modelo e a `display` compõe o quadro sob `model_lock()`; o nível de bateria vai ao BLE só quando muda (`src/main.c`, `src/model/model_lock.c`, `src/vue/vue.c`). RAM −1.280 B.
- Nenhum watchdog (o legacy tinha um de 4 s): `task_wdt` com um canal de 4 s para a `main_loop` e outro para a `display`, sobre o WDT do nRF; ao expirar, loga a thread travada e reinicia. O boot alimenta o WDT quando ele sobrevive a um reset por software (`src/main.c`, `prj.conf`). FLASH +1.116 B.
- O aparelho nunca desligava (o legacy desliga pelo STC3100 depois de 15 min sem atividade e pelo menu): `power_scheduler` portado do legacy, com ping a cada posição processada em CRS/PRC, `stc3100_shutdown()` (`REG_MODE = 0`, `REG_CONTROL = 0x01`), a atividade salva zerada antes e o item "Power Off" no menu; na USB, nova tentativa 15 min depois (`src/model/power_scheduler.c`, `src/drivers/sensors/stc3100.c`, teste `test_power_scheduler` com 8 casos).

### Adicionado

- **Alarme da bicicleta e detecção de queda** (`src/model/incident.c`, `test_incident` com 20 casos; [06](docs/06-algoritmos.md#alarme-da-bicicleta-e-detecção-de-queda)). **Não é equipamento de segurança**: erra nos dois sentidos, e ninguém deve pedalar diferente porque está ligado. O que está escrito no módulo, nos testes e na tela diz isso.
  - A **queda** precisa de três coisas em sequência, porque cada uma sozinha é fato corriqueiro: um pico acima de 6 g, a bicicleta parada, e o aparelho quieto por 8 s. Só então começa a contagem de 30 s, e qualquer tecla — ou voltar a pedalar — cancela. Quem está bem sempre cancela.
  - O **alarme** é armado pelo menu quando o ciclista se afasta da bicicleta, espera 5 s enquanto ele ainda mexe no cadeado, e toca com 0,15 g mantidos por um segundo.
  - O serviço de sensores passou a acompanhar o **pico** do módulo da aceleração a 50 Hz e a publicá-lo uma vez por segundo: um impacto dura cerca de um décimo de segundo e a média de um segundo o enterraria.
  - **Duas telas novas** (a 36ª e a 37ª), que tomam a frente como a da atualização, com o triângulo de aviso, o que houve e os segundos que faltam.
- **Radar traseiro** (`src/model/radar.c`, `radar_wire.c`, `src/rf/ble_radar_client.c`; `test_radar` com 20 casos). O legacy é anterior ao Varia. O modelo — a lista do que vem atrás, com distância, velocidade de aproximação e nível de ameaça — é puro e coberto por testes; os dois rádios ficam de fora dele.
  - **BLE:** cliente do serviço que o Garmin Varia expõe, com descoberta, inscrição e o `disc_params`/`end_handle` que os outros clientes precisaram em 2026-09-20. **O formato não foi conferido em aparelho**: a Garmin não o publica, o que está aqui segue o que os projetos abertos relatam, e o número com mais chance de estar errado — a unidade da velocidade de aproximação — mora num lugar só (`RADAR_VARIA_SPEED_KMH`).
  - **ANT+:** só a tubulação. O perfil Bike Radar está sob a ANT+ Shared Source License e este repositório é público, então as chaves do canal vêm do Kconfig (zeradas aqui) e a decodificação das páginas é uma função declarada em `rf/radar_ant.h` e definida num arquivo que o `.gitignore` mantém fora. Sem ele, uma definição fraca devolve falso e o BLE segue funcionando; com ele, o CMake compila e a definição forte assume.
  - Um veículo mantém o lugar entre quadros, a lista fica ordenada pela distância, e uma marca que deixou de ser reportada **fica vazada por 4 s** em vez de sumir — um radar perde quadro, e marca piscando é pior que marca parada.
  - Na tela: uma faixa na borda direita das páginas com desenho (mapa, perfil, subida), com a bicicleta embaixo e uma marca por veículo na distância dele, colorida pela ameaça; e um ponto na barra de estado em todas as páginas. Nas páginas de dados a faixa cobriria as unidades, então elas só têm o ponto.
- **As subidas do percurso, e a tela que sobe sozinha no pé de cada uma** (`src/model/climb.c`, `test_climb` com 16 casos; [06](docs/06-algoritmos.md#subidas-do-percurso-climbpro)). O legacy mostra o percurso inteiro e a subida total, e nada sobre a subida em que o ciclista está: numa estrada de montanha o perfil do percurso é uma linha reta com um calombo, e o que serve é o calombo ocupando a tela.
  - O percurso é varrido uma vez, quando abre, **afinado** para 512 amostras (cerca de 200 m num percurso de 100 km; 4 KB em vez dos 32 KB do percurso), com a distância seguindo ponto a ponto para o afinamento não cortar curva.
  - Uma subida abre e fecha com 10 m de histerese, engole um falso plano de até 1 km e um terço do ganho, e só conta a partir de 500 m, 30 m de ganho e 3 % de média. A categoria vem do ganho, na escala do ciclismo: 80 m é quarta, 800 m é fora de categoria.
  - **Tela nova, a 33ª**, que aparece sozinha no pé da subida e devolve o mapa no topo: o perfil da subida com o trecho pedalado em verde e o resto colorido pela inclinação, quanto falta de distância e de altimetria, a média do que resta e a inclinação dos próximos 200 m. Entre duas subidas mostra a distância e o tamanho da próxima. Uma notificação anuncia o pé de cada subida.
- **Cronômetro, pausa automática e voltas** (`src/model/activity.c`, `test_activity` com 19 casos). O legacy grava do momento em que liga até desligar, sem início, sem pausa e sem volta; o port passa a ter o que o mercado tem e o que um arquivo FIT carrega:
  - o **cronômetro** para sozinho depois de 3 s abaixo de 1,5 km/h e volta acima de 3 km/h, de modo que a média é a do passeio e não a do semáforo. A histerese existe para a bicicleta empurrada a 2 km/h não fazer o cronômetro oscilar. `CONFIG_GNSS_AUTO_PAUSE` desliga;
  - **voltas** a cada `CONFIG_GNSS_AUTOLAP_M` metros (5 km de fábrica) e pelo **toque longo na tecla esquerda**, de qualquer página de dados, com notificação;
  - **descida**, que o legacy nunca contou, com a mesma banda morta de 2 m da subida de `attitude.c`;
  - **energia** em kcal a partir dos watt-segundos, e as médias de velocidade, potência, frequência e cadência sobre o tempo em movimento. Uma cinta que cai pela metade do passeio já não derruba a média da metade em que respondeu.
  - **Tela nova** (a 31ª, `docs/telas/README.md`): quarta página do anel do CRS, com a volta em curso, o tempo em movimento, média e máxima, descida e energia, e **PAUSADO** no canto quando o cronômetro para.
- **A atividade gravada em FIT** (`src/model/fit_encode.c` e o serviço de armazenamento; `test_fit_encode` com 15 casos e `test_e2e_activity` com 6), um arquivo por passeio ao lado do `@DDMMYY.txt` do legacy, porque é o formato que Strava, Garmin Connect e Komoot leem sem conversor ([09](docs/09-armazenamento-usb.md#arquivo-da-atividade-em-fit)).
  - Um `record` por época com hora, posição, altitude, distância, velocidade, potência, frequência e cadência; um `event` de cronômetro em cada pausa e cada retomada, para o leitor saber que o buraco nos pontos é parada e não receptor mudo; uma `lap` por volta; e no fim a `session` e a `activity`. O arquivo é `<DDMMYY>.FIT` na raiz, com uma letra a mais no segundo passeio do dia.
  - O codificador é C puro sobre um buffer de quem chama, sem arquivo e sem alocação: um passeio de quatro horas a 1 Hz tem cerca de 380 KB e nunca cabe na RAM.
  - **O cabeçalho é reescrito no fim, com o tamanho que o arquivo ficou, e isso não estraga o CRC.** Os dois últimos bytes de um cabeçalho FIT são o CRC dos doze anteriores, e alimentar uma mensagem seguida do próprio CRC deixa esse CRC em zero: o estado depois de qualquer cabeçalho válido é zero, diga ele o tamanho que disser. A primeira versão levava a diferença entre os dois cabeçalhos através dos dados, pela linearidade do CRC; o teste de mutação mostrou que aquele trecho era código morto, e ele saiu (33 linhas a menos). A propriedade tem teste próprio, além do CRC final conferido contra o cálculo direto em 33 tamanhos de arquivo.
  - O telefone continua sem poder mandar uma atividade para o aparelho (`file_policy.c`): quem grava um passeio é o aparelho.

- **Formato de percurso próprio e GPX direto** (`src/model/route_file.c`, `src/model/gpx_scan.c`, `tools/route_convert.py`, com `test_route_file`, `test_gpx_scan` e `test_e2e_route`): o aparelho passou a aceitar três formatos e escolhe pelo conteúdo do arquivo. O **`.RTE`** é o deste projeto — binário, versionado, com cabeçalho de 64 B que dá nome, distância, subida e caixa ao menu sem abrir o arquivo, **CRC-32** do corpo e lista de curvas com o nome da rua; um percurso de 100 km a cada 10 m ocupa 100 KB contra 303 KB do texto do legacy e 1 a 3 MB do GPX. O **GPX e o TCX entram direto**, sem conversão: o leitor é uma máquina de estados que varre os bytes conforme chegam, aguenta namespaces, extensões desconhecidas, atributos em qualquer ordem e o arquivo em pedaços, e alimenta o watchdog porque um arquivo grande leva segundos. As curvas do `.RTE` viram a instrução da tela quando não há telefone navegando. O conversor em `tools/` traduz o que o Strava, o Komoot e o RideWithGPS exportam e confere o próprio resultado (`--selftest`).

- **Percurso enviado pelo telefone**, como num Garmin (`src/model/file_policy.c`, `src/rf/file_xfer.c`, `boards/nrf54lm20dk_nrf54lm20a_cpuapp.conf`, `svc/storage/storage_svc.c`, com `test_file_policy` e `test_e2e_route`): o aplicativo manda o arquivo pelo grupo de arquivos do mcumgr, no mesmo enlace SMP da atualização, e o aparelho decide o que aceita — percursos e segmentos entram, a atividade só sai (quem a escreve é o aparelho), nada fora da raiz do armazenamento e nada que o firmware não saiba ler. Quando o arquivo chega, a tela avisa e o armazenamento lista o cartão de novo, então o percurso novo aparece no menu sem reiniciar.
- **Perfil de elevação do percurso** (`src/model/route_profile.c`, tela 30, `test_route_profile`): o relevo do percurso inteiro em até 120 colunas, com o trecho já pedalado destacado (contorno no tema de uma cor), a posição do ciclista, a subida que ainda falta — contada como o legacy conta, só as subidas —, o que resta em quilômetros e as altitudes mínima e máxima. Chega-se a ela no modo PRC com toque longo na tecla direita, e o mesmo toque volta ao mapa.

### Corrigido

- Decimação do percurso deixava o começo esparso e o fim denso (`src/model/parcours.c`, achado pelo `test_e2e_route`): ao encher o array, o carregador dividia os pontos pela metade mas voltava a aceitar **todos** os seguintes, então um curso de 100 km com 10.000 pontos entrava com o primeiro quilômetro representado por um ponto e o último por dezenas. O passo agora vale também depois de cada divisão, e o último ponto do arquivo entra sempre: o mesmo curso entra com 314 pontos igualmente espaçados, 4.992 m de subida contra os 5.000 reais, e a navegação deixa de dizer que o ciclista está fora da rota nos primeiros quilômetros.

- Percursos e segmentos com resolução de verdade na placa nova (`zephyr_app/Kconfig`, `boards/nrf54lm20dk_nrf54lm20a_cpuapp.conf`, `model/parcours.h`, `model/segment.c`): os pontos que cabem na memória viraram opções (`CONFIG_GNSS_ROUTE_POINTS`, `CONFIG_GNSS_SEGMENT_POINTS`), e o alvo do nRF54LM20A, que tem 512 KB de RAM, guarda **4.000 pontos de percurso** e 768 por segmento. Um percurso de 100 km passa a ter um ponto a cada 25 m, a densidade que o Strava e o Komoot exportam, no lugar de um a cada 200 m que os 500 pontos antigos deixavam; o segmento mais longo dos exemplos (1.291 pontos) entra com um ponto a cada 16 m. O alvo nRF52840, com um quarto da RAM, fica com os valores de antes. RAM do nRF54LM20 DK: 375.404 B (71,7 %), ainda com 144 KB livres.

- Peça do armazenamento escolhida pela ficha (`docs/15`, `docs/14`, `docs/19`, overlay do nRF54LM20 DK): **Macronix MX25R6435F**, flash NOR SPI de 8 MB. As contas que levaram a ela: os 138 segmentos reais somam 0,44 MB (a estimativa anterior, de 7 MB, estava errada por 16 vezes), os percursos 0,05 MB e um pedal de 10 h deixa 1,9 MB de log, então 8 MB guardam tudo isso e cerca de 35 pedais longos. Contra a Winbond W25Q128JV de 16 MB, a Macronix gasta **cinco vezes menos energia para apagar um setor** (0,18 contra 0,90 mA·s) e fica em 7 nA em deep power-down contra 1 µA, aceita de 1,65 a 3,6 V e é a mesma peça que o nRF54LM20 DK traz, então o que se depura na bancada é o que vai na placa; o Zephyr ainda tem tratamento próprio para ela, que o overlay passou a usar (`mxicy,mx25r-power-mode = "low-power"`). A W25Q128JV fica como alternativa pino a pino, para o caso de falta ou de o log crescer. Falta conferir preço e estoque.

- USB no alvo da placa nova (`src/svc/usb/usb_svc.c`, `src/app/app_cmd.c`, overlay e `.conf` do nRF54LM20 DK): um serviço novo, o oitavo, sobre a pilha `device_next` do Zephyr. Com o cabo ligado o PC vê uma **porta serial** que aceita os mesmos comandos do legacy que o rádio aceita — o tratamento saiu do serviço de rádio para `app_cmd.c` e serve aos dois —, e no **modo USB** (menu ou `$DWN,16`) o disco do ciclista vai para o PC pela classe de armazenamento em massa, depois que o serviço de armazenamento desmonta; sair do modo pede reset, como no legacy. A interrupção do CDC só enfileira bytes num `ring_buf`, e a thread lê, como as outras interrupções deste firmware. Pilha medida com `CONFIG_STACK_USAGE`: ~0,9 KB de 2.048 B. Os identificadores são os de teste do pid.codes (VID 0x1209, PID 0x0001), a trocar por um número próprio antes de qualquer venda. Não testado com cabo.

- Armazenamento em **flash NOR soldada** no alvo da placa nova, no lugar do cartão (`boards/nrf54lm20dk_nrf54lm20a_cpuapp.overlay` e `.conf`, docs [13](docs/13-placa-nova.md), [14](docs/14-hardware-placa-nova.md), [15](docs/15-avaliacao-componentes.md), [19](docs/19-lista-de-compras.md) e [09](docs/09-armazenamento-usb.md)): decisão do dono em 2026-09-20, porque o SD NAND de 1 Gbyte sai por US$ 40 e o aparelho grava dezenas de megabytes (2,4 MB num pedal de 10 h, com os 138 segmentos de exemplo somando 7 MB). O firmware monta o FatFs sobre um `zephyr,flash-disk` na partição da NOR, com o mesmo `/SD:` de sempre, e formata na primeira montagem se a peça vier em branco; o build usa o MX25R6435F de 8 MB que o nRF54LM20 DK traz no mesmo `spi00`, então é a pilha da placa nova que compila e roda. O alvo nRF52840 DK, que representa a V3, segue com o cartão. Falta escolher a peça de 32 MB e conferir preço, tensão e consumo.

- Comandos do legacy pelo rádio (`src/model/cmd_parser.c`, `src/svc/radio/radio_svc.c`, `test_cmd_parser`): o leitor das sentenças `$LOC`, `$HRM`, `$CAD`, `$ANCS`, `$DWN`, `$QRY`, `$BTN` e `$DBG` do `VParser` do legacy, como módulo puro, ligado ao Nordic UART Service. A posição do PC (`$LOC`) entra marcada como simulada e o modelo a prefere ao receptor por 6 s, que é a prioridade SIM > GPS do legacy; batimentos e cadência entram como sensor externo; `$BTN` aperta uma tecla; `$ANCS` e `$DBG` viram notificação. Os comandos que destroem dados (formatar, `mkfs`) ou derrubam o aparelho de propósito passaram a ser **recusados pelo rádio** — no legacy qualquer periférico que se dissesse stravaAP podia formatar o cartão —, e só saem do menu, onde o ciclista confirma na tela. Falta responder ao `$QRY`, que lista e envia arquivos em blocos. Não testado com o dongle nem com o `tools/zpm`.

- Mapa do percurso e dos segmentos na tela (`src/model/map_project.c`, `src/svc/model/model_ui.c`, `src/model/segment.c`, `test_map_project`): o modelo projeta as posições em por mil da janela, com o ciclista no meio, a mesma escala nos dois eixos (a janela do mapa é 240 × 107) e um passo que encaixa o percurso nos 160 pontos e cada segmento nos 64 que a tela guarda. O zoom segue o `Zoom.cpp` do legacy em cinco passos (100 m a 2.500 m de meia-janela, com o nível 2 igual ao padrão do legacy) e a barra de escala é um número redondo. Os segmentos mostrados são os que estão correndo, por pontuação, e depois os mais próximos que já estão carregados, como o `Vue::afficheSegment()` do legacy escolhe. Antes o retrato saía com `nseg` e `route.n` em zero, e a tela do PRC dizia "sem percurso". Não visto em painel.

- Atualização de firmware pelo ar, por Bluetooth (`Kconfig.sysbuild`, `sysbuild/mcuboot.conf`, `boards/nrf54lm20dk_nrf54lm20a_cpuapp.conf`, `src/rf/dfu.c`, `src/model/dfu_state.c`, tela nova, `test_dfu_state`): **MCUboot** pelo sysbuild e **mcumgr SMP sobre BLE**, as peças oficiais do NCS v3.3.0, com a configuração de transporte do exemplo `smp_svr` (remontagem, MTU de 498 B, buffer de 2475 B). O aplicativo do telefone não é deste projeto: qualquer cliente SMP serve. Só o alvo **nRF54LM20A** leva o bootloader, porque os slots de 920 KB comportam os ~525 KB do firmware, enquanto no nRF52840 o slot tem 481 KB contra 480 KB de firmware; o `Kconfig.sysbuild` liga o MCUboot pela placa, com o `default` antes do `source` porque num `choice` o primeiro que se aplica vence. O módulo do firmware confirma a imagem que está rodando (`boot_write_img_confirmed()`) quando os serviços sobem, publica a fase e a porcentagem num canal do zbus e **recusa** a atualização em duas situações: com uma atividade em curso, porque o reset perderia o passeio, e com menos de 30 % de bateria fora do carregador. A tela de atualização toma a frente enquanto a imagem chega e devolve as páginas no fim (telas 28 e 29 do renderizador). A imagem ainda é assinada com a **chave de desenvolvimento do MCUboot**, que é pública: antes de sair da bancada é preciso gerar uma chave ED25519 própria, guardada fora deste repositório. Não testado em placa ([07](docs/07-radio-ant-ble.md#atualização-por-ble-dfu)).

- Carga dos segmentos pelos arquivos do legacy (`segment.c`): a varredura passou a ser da **raiz** do cartão e só lê os nomes, sem abrir nada, como o legacy faz; a posição de início sai do nome e é ela que o alocador usa para decidir a carga a menos de 300 m; e os pontos vêm do texto `lat ; lon ; rtime ; alt`, com o tempo de cada ponto relativo ao primeiro e a subida total tirada da diferença de altitude. O formato binário próprio (`/SD:/segments/*.seg`), que nenhuma ferramenta gerava, saiu.
- Leitor dos segmentos do legacy (`src/model/segment_file.c`, `test_segment_file`): o nome de 12 caracteres (`LLLLL#OO.OOO`) carrega a posição de início em base 36, como o `calculePos()` de `libraries/utils/utils.c:201-227`, e o conteúdo é texto com uma linha `<Name>` e pontos `lat ; lon ; rtime ; alt`. O teste confere os **138 segmentos reais** de `tools/TDD/DB`: para cada arquivo, a posição do nome tem de bater com o primeiro ponto de dentro. A conversão inversa (posição para nome) também está lá, para quando o aparelho gravar segmentos. A posição é decodificada em `double` e devolvida em `float`: o legacy divide o inteiro já em `float` e perde cerca de 4 m numa longitude de 34.696.890.
- GNSS por UBX: driver próprio do u-blox M10 em `zephyr_app/modules/gnss_drivers` (compatível `u-blox,max-m10`, sobre o `modem_ubx` do Zephyr num pipe de UART), porque o NCS v3.3.0 só traz M8 e F9P e o M10 não aceita as mensagens `UBX-CFG-*` antigas. Os quadros e as mensagens ficam em C puro (`ubx_m10.c`): checksum de Fletcher de 8 bits, `UBX-CFG-VALSET` e `VALGET` com o tamanho do valor tirado da chave, `UBX-RXM-PMREQ`, `UBX-CFG-RST`, `UBX-NAV-PVT` e `UBX-NAV-SAT`, tudo conferido contra a descrição de protocolo SPG 5.30 (`test_ubx_m10`, 20 casos, vetores de uma implementação independente). O driver configura nas camadas RAM e BBR (o standby apaga a RAM do receptor), pede uma época por segundo com satélites, mantém o modelo `portable`, desliga o pulso de tempo e rastreia em LEAP (`CFG-PM-OPERATEMODE` = 2), com potência plena antes de um lote de comandos, porque o receptor perde mensagens enquanto faz o ciclo do LEAP.
- Máquina de energia do receptor em `src/svc/gnss/gnss_power.c` (`test_gnss_power`, 16 casos), seguindo `docs/16`: backup fora de CRS, PRC e DBG, aquisição até o primeiro fix, LEAP no rastreio, potência plena depois de 10 épocas sem fix e volta ao LEAP depois de 60 s com fix. Silêncio do receptor de 10 s manda a configuração de novo e de 30 s puxa o RESET_N (ou manda `UBX-CFG-RST`), com notificação na tela, como o legacy avisava ao reiniciar a UART (`legacy/source/sensors/GPSMGMT.cpp:143-176`). O serviço passou a receber a época pela caixa de entrada e a publicar da sua thread: nada que espere resposta do receptor pode rodar no workqueue do modem, onde a resposta chega.
- Carregador solar: driver próprio do AEM10900 em `zephyr_app/modules/gnss_drivers` (compatível `e-peas,aem10900`, com o prefixo `e-peas` na lista de fabricantes do módulo) pela API de carregadores do Zephyr. Na partida confere o número da peça, passa à configuração por I2C para carregar até 4,05 V com a placa ligada (os pinos carregam até 3,90 V; os demais registradores ficam no valor de fábrica, que é o dos pinos) e liga o monitor de potência; se o AEM reinicia e volta aos pinos, grava de novo. O serviço de energia lê a cada 10 s se o painel carrega, a potência e o limiar em vigor, junta isso ao nPM1300 na máquina de carga e devolve o AEM aos pinos antes de cortar a energia. A potência em mW espera o fator θ·L da e-peas (`apm-scale-pj`, 0 até lá). Unidades da ficha em `aem10900_regs.h`, `test_aem10900` (7 casos), mutação 10 de 10. Não testado com o AEM10900.
- Carregador: o nPM1300 no devicetree do nRF54LM20 DK (em 0x6B no `i2c24`, interrupção do GPIO3 em P0.04, como as amostras da Nordic ligam o EK), com os trilhos do `docs/14` travados (BUCK1 em 1,8 V, BUCK2 em 3,0 V sempre ligado, LDO1 como chave do cartão, LDO2 em 3,3 V para a luz) e a carga de 600 mA até 4,20 V com o NTC do pack. O serviço de energia recebe os eventos do PMIC (VBUS, fim de carga, erro) como mensagens, lê o carregador a cada evento e a cada 10 s, passa o VBUS à máquina de sistema, sobe o limite do VBUS ao que a fonte USB-C oferece e calcula o estado da máquina de carga do `docs/16` (`src/svc/power/charge.c`, `test_charge` com 11 casos, mutação 7 de 7); pausa térmica e falha viram notificação. A thread `ui` liga a alimentação da luz (alias `backlight-supply`) junto com o PWM. Não testado com o nPM1300.
- Medidor de bateria: driver próprio do MAX17262 em `zephyr_app/modules/gnss_drivers` (compatível `adi,max17262`) pela API de fuel gauge do Zephyr, com a configuração ModelGauge m5 EZ depois de cada reset do medidor e as unidades da tabela 2 da ficha (`max17262_regs.h`). O driver da árvore do Zephyr (`maxim,max17262`) grava a capacidade e a corrente de fim de carga sem esses passos, devolve capacidades erradas e não reconhece o tempo desconhecido. O serviço de energia lê o medidor a cada 10 s (alias `fuel-gauge0`), publica tensão, corrente, carga, temperatura e autonomia em `power_status` quando algo da tela muda, avisa bateria fraca a 10 % (de novo só depois de 15 %) e, a 0 % descarregando, pede o desligamento à máquina de sistema (`src/svc/power/battery.c`); o legacy não tinha regra de bateria baixa. O nRF54LM20 DK ganhou o barramento de energia no `i2c24` (os pinos P1.11 e P1.12 das amostras da Nordic para o nPM1300 EK) com o MAX17262 em 0x36. A pilha da thread `power` subiu para 3072 B (~1,1 KB medidos). Testes de host `test_max17262` e `test_battery` (10 casos cada), mutação 15 de 15. Não testado com o medidor.
- Interface no firmware. Driver próprio da tela em `zephyr_app/modules/gnss_drivers`, um módulo do Zephyr dentro da aplicação, para o JDI LPM027M128B e C em 3 bits e a Sharp LS027B7DH01 em 1 bit: retrato como a V3 (`setRotation(3)`), RGB565 quantizado pela mesma regra do renderizador de host (`memlcd_pixel.h`, compartilhado), quadro guardado no formato do fio (36.482 B no JDI, os 12.482 B do legacy na Sharp), só as linhas que mudaram no fim de cada quadro do LVGL, COM pelo EXTCOMIN num timer ou pelo bit VCOM do SPI (V3), sequências de partida e desligamento das fichas. A thread `ui` roda o LVGL (6 KB de pilha, cerca de 4,7 KB medidos com `CONFIG_STACK_USAGE`): retrato do modelo, notificações, modo, teclas, telas de USB e de desligamento com o progresso dos serviços, tema e luz guardados em `ui/prefs`; as ações viram comandos do zbus. Teclas por `gpio-keys` e `zephyr,input-longpress` (toque longo de 1 s), publicadas em `input`. A máquina da luz de `docs/16` em `src/svc/ui/backlight.c`, com PWM pelo alias `backlight` e o COM a 120 Hz no JDI com a luz acesa. O nRF54LM20 DK ganhou o LPM027M128B no `spi22`, os botões e o LED 1 no papel da luz; o nRF52840 DK, a LS027 no driver novo com VCOM serial, como o esquema da V3 (R13, R16, R17, C43). Testes de host `test_memlcd` (19 casos) e `test_backlight` (11 casos), mutação 22 de 22. O LVGL do firmware espelha o `lv_conf.h` do renderizador: só os formatos RGB565 e A8 no renderizador (as 58 imagens saem iguais byte a byte), sem log, sem temas, só rótulos; `CONFIG_LV_Z_BITS_PER_PIXEL=16` evita o buffer de desenho dobrado. O workqueue do sistema e a thread de entrada passaram a 2048 B. A interface ocupa cerca de 151 KB de flash no nRF54LM20 DK. Build sem aviso nos dois DKs, com e sem `ANT=1`; não testado em painel nem na placa.
- Decisão do dono em 2026-09-19: a tela é o JDI LPM027M128B, comprado no AliExpress, com a Sharp LS027B7DH01A de reserva (`docs/10`, `docs/18`, `README.md`); a lista de compras não muda.
- Renderizador de host: mede a pilha das telas (7.359 B em x86-64) e usa o header de quantização do driver; `ui_show_pages()` na API da interface, para sair da tela de USB.
- `docs/telas/`: as 29 telas da interface, uma imagem por tela nos temas de 8 cores e preto e branco (geradas por `tools/ui/render_screens.py`, que ganhou a opção `--screens`), e um README com o que cada tela mostra, quando aparece, as teclas e a origem no legacy.
- Base da arquitetura de `docs/16`, descrita no `docs/05`: sete serviços com thread própria (`sensors`, `model`, `gnss`, `radio`, `ui`, `storage`, `power`), cada um dormindo numa caixa de entrada (`k_msgq` enchida por listeners do zbus, sem alocação depois do boot); 20 canais do zbus com as mensagens de `include/app/app_events.h`; máquina de sistema no SMF (Partida, Ligado, MSC, Desligando com a resposta de cada serviço por até 5 s, Desligado com ship mode do nPM1300 ou System OFF) e máquina de modos (CRS, PRC, FEC, Zwift, DBG) com as entradas e saídas de `boucle__change_mode()`; um canal de 4 s do `task_wdt` por thread de serviço. Os serviços acham o hardware por aliases do devicetree (`gnss`, `baro0`, `imu0`, `mag0`, `light0`, disco `SD`) e usam as APIs do Zephyr: GNSS (posição por época, satélites, workqueue próprio do modem), sensores (acelerômetro a 50 Hz com média de 50 amostras, barômetro a 10 Hz, magnetômetro e luz a 1 Hz, como o legacy), FatFs de verdade (log pelo `sd_logger`, segmentos, lista de percursos). O nRF54LM20 DK ganhou os periféricos da placa nova (u-blox por `gnss-nmea-generic` até o driver UBX, BMP585, BMI270, MMC5633NJL, OPT3001) nos pinos de referência do `docs/14`. Pilhas medidas com `CONFIG_STACK_USAGE`, todas com pelo menos 1 KB de folga; a thread RX do BT subiu para 3072 B. Build sem aviso nos dois DKs; com `ANT=1`, só o do símbolo obsoleto. Não testado na placa.
- `src/svc/sensors/tilt.c`: inclinação e rolagem da média de 50 amostras e rumo compensado, pelas equações do AN4248, nos eixos da placa, e a rugosidade do legacy (desvio médio absoluto, em contagens de ±4 g); o rumo do legacy não compensava a inclinação (`fxos.cpp:796-821`), diferença registrada no `docs/06`.
- Testes de host `test_sys_fsm` (16 casos, com o `lib/smf/smf.c` do Zephyr do NCS e um shim de `sys/util.h`) e `test_tilt` (8 casos, contra leituras construídas por rotação e o laço de rugosidade do legacy); mutação: 6 de 6 na máquina de sistema, 3 de 3 no `tilt.c`, 1 de 1 no `power_scheduler`, e as do `ui_fmt.c` refeitas (6 de 6) com um executor que chama o `cmake` e o `ctest` direto.
- Interface da placa nova em LVGL (`zephyr_app/src/ui`, `zephyr_app/include/ui`), em C sem Zephyr: 29 telas em retrato nos temas de 8 cores (JDI) e preto e branco (Sharp), com os arranjos, os campos e os formatos do legacy (`VueCRS.cpp`, `VuePRC.cpp`, `VueFEC.cpp`, `VueGPS.cpp`, `VueDebug.cpp`, `Menuable.cpp`, `Screenutils.cpp`): páginas CRS com 0, 1 ou 2 segmentos em cada estado, mini-mapa e `partner`, PRC, FEC, GNSS procurando, DBG, menu, lista de percursos, ajustes, sensores, pareamento, edição de valor, tela e luz, formatação, energia, USB, partida, desligamento e fila de notificações. Recebe um retrato do estado (`ui_model_t`) e devolve ações. Fontes DejaVu Sans de 1 bit geradas por `tools/ui/font_gen.py` (cerca de 24 KB). Renderizador de host (`zephyr_app/tests/ui`, `tools/ui/render_screens.py`) que desenha cada tela como o painel mostra, em `docs/img/telas-lvgl/`, e confere cores, textos fora da caixa e navegação: 58 quadros, 0 problemas, 23,7 KB de heap do LVGL no pico. Conjunto de host `test_ui_fmt` (10 casos) contra a transcrição de `_fmkstr` e `_secjmkstr`. Ainda fora do firmware e sem driver de tela; não testado na placa.
- `docs/19-lista-de-compras.md`: lista de compras do protótipo da placa nova, com cada peça validada em duas passagens, a compra (PN, estoque, preço e situação na DigiKey em 2026-09-18) e a integração (pinos, máximos absolutos, externos, altura, driver no NCS v3.3.0): trocas, 18 correções de integração, lista por bloco, passivos com a capacitância efetiva pelo DC bias, placas de avaliação, custo estimado (cerca de US$ 215 por placa) e compras fora da DigiKey (AEM10900, SD NAND, bateria, Tag-Connect). A Mouser não foi conferida.
- `docs/18-interface-telas.md`, `tools/docs/screens_drawing.py` e `docs/img/telas/`: interface da placa nova. LVGL (9.5 "dev" no NCS v3.3.0) sobre um driver de tela próprio, porque o `jdi,lpm013m126` do Zephyr aceita até 255 px por eixo, só linhas inteiras e o cabeçalho de 8 + 8 bits, e o LPM027M128C tem linhas de 400 pixels, cabeçalho de 6 + 10 bits e 16 clocks de pausa; o driver guarda um quadro de 3 bits, gira para o retrato e envia só as linhas que mudaram, e atende também o Sharp em 1 bit. Paleta de 8 cores sem informação só por cor, grade de 2 × 7 do legacy com barra de estado, fontes de 1 bit com acentos, 19 telas com maquetes geradas em 240 × 400 só com as cores do painel, botões com toque longo proposto, ritmo de atualização, EXTCOMIN e luz, memória e diferenças para o legacy.
- `docs/17-dispositivos-ble-ant.md`: catálogo dos dispositivos externos de ciclismo, com o perfil ANT+ (tipo de dispositivo do SDK FIT) e o serviço BLE (UUID do SIG) de cada um, o que o `sdk-ant` e o NCS já trazem (perfis HRM, BSC, potência parcial e páginas comuns; clientes de FC, bateria, NUS, hora, HID e ANCS; nenhum cliente de CSC, CPS, FTMS nem LNS), prioridades (P1 para o que o legacy usa, radar em P2, e-bike Bosch pela Live Data Interface aberta, Di2 e suspensão fora), os limites de 15 canais ANT e das conexões BLE, a convivência dos dois rádios ainda não testada pela Garmin no nRF54LM20, o pareamento e o custo da licença comercial do ANT (US$ 0,08 por unidade, mínimo de US$ 800 por semestre).
- `docs/16-arquitetura-firmware.md`: arquitetura-alvo do firmware para a placa nova. Seis serviços (energia, GNSS, sensores, rádio, armazenamento, configurações), seis threads com prioridade e pilha de partida, catorze canais no zbus, nove máquinas de estado no SMF (sistema e energia, modo, gravação, GNSS, sensor externo, carga, interface, luz do display e a de segmentos do legacy) com as origens no legacy, sequências de partida e de desligamento, energia por estado, armazenamento, comunicação, atualização pelo MCUboot com o mapa de partições do nRF54LM20, falhas, migração do port e decisões em aberto. Os blocos do Zephyr e do NCS usados foram conferidos no NCS v3.3.0 local; entre eles, o `task_wdt` tem 5 canais por padrão e não há cliente BLE pronto para CSC, CPS, LNS nem FTMS.
- `docs/15-avaliacao-componentes.md`: avaliação dos componentes da placa nova, bloco a bloco, com números de datasheet. Carga: nPM1300 no USB-C e AEM10900 no painel (topologias BQ25798, painel no VBUS e harvester no VSYS descartadas), MAX17262 na célula, o USB bloqueando a carga solar pelo DIS_STO_CH e indutor de 6,8 µH no AEM10900. GNSS: MAX-M10N-10B em LEAP (13,5 mW contra 46,8 mW do MAX-F10S; cerca de 21 mW no aparelho e 320 h sem sol), com o F10S no mesmo footprint. Também módulo do MCU, display com o Sharp previsto no mesmo conector, antena, sensores, SD NAND soldado, USB-C IPX8 com ESD751 no VBUS, bateria com dois NTC, tradutor de nível e a bancada antes do layout.
- `docs/14-hardware-placa-nova.md`: especificação técnica preliminar da placa nova, com estado das decisões, arquitetura, árvore de alimentação (BUCK1 3,0 V, BUCK2 1,8 V para o GNSS, VBUSOUT do nPM1300 no VBUS do nRF, TPS7A02 no backup do GNSS), estados de energia (cerca de 34 µA desligado), lista de materiais, GNSS com tradutor de nível TXU0204, endereços I2C sem conflito, alocação de 38 GPIO, PCB de 55 × 97 × 1,0 mm em 4 camadas com zonas, empilhamento de 17 mm na caixa de 19 mm, regras de layout e ordem de bring-up. O desenho passou a ter o sensor de luz embaixo à esquerda, o IMU mais baixo e o respiro do barômetro fora da sombra da bateria, e o `docs/13` deixou de pedir resistores de 5,1 kΩ no CC (o nPM1300 tem o Rd interno).
- `docs/img/placa-nova-caixa.svg` e a seção "Como fica o aparelho" do `docs/13`: desenho em escala do aparelho proposto, a partir da caixa da V3 (62 × 104 × 19 mm, JDI colorido, 6 módulos solares na frente inclinada e nos chanfros laterais, antenas GNSS no topo, BM20C no canto oposto, USB-C na base), gerado por `tools/docs/case_drawing.py`. A área de painel do `docs/13` passou a ser a que cabe no desenho (11 cm² de módulos, cerca de 7 a 9 cm² equivalentes), no lugar dos 10 a 15 cm² estimados antes.
- Build com ANT (`ANT=1` no `fw.sh` e no `build.bat`): o add-on `sdk-ant` v2.1.1, clonado em `C:\ncs\sdk-ant`, roda sobre o NCS v3.3.0 como módulo extra do Zephyr, com `zephyr_app/modules/ant_ncs33_compat` (religa `SOC_SERIES_NRF52X` e `SOC_SERIES_NRF54LX`, obsoletos no NCS v3.3.0) e `zephyr_app/ant.conf`; `rf_ant_init()` (`src/rf/ant/ant.c`, port do `ant_stack_init()` do legacy) sobe a pilha e grava a chave ANT+ antes do BLE. Compila nos dois alvos (+28,6 KB de FLASH e +4,6 KB de RAM no nRF52840); o build sem `ANT` não muda; não testado em placa. `docs/07` ganhou o mapa do que o ANT e o ANT+ podem integrar ao port.
- Testes de host do port (`zephyr_app/tests/host/`, rodados por `tools/fw/host_tests.sh`): Unity 2.6.1 + CTest com o GCC do PC, shims do Zephyr, sistema de arquivos em memória e HAL falso do GPS; 6 conjuntos, 42 casos (`vecteur`, `power_zone`, `suffer_score`, `nmea_parser`, `sd_logger`, `gps_mgmt`), com oráculo do legacy e mutação conferida nas correções.
- `tools/fw/`: `ncs_env.sh` e `ncs_env.bat` (ambiente do NCS a partir do `environment.json` do toolchain) e `fw.sh` (build, flash, recover, devices, size).
- `tools/docs/`: `mermaid_check.py` (extrai e renderiza os diagramas com o mermaid-cli local e aponta diagramas em texto puro) e `links_check.py`.
- `zephyr_app/sysbuild.conf` com `SB_CONFIG_PARTITION_MANAGER=n`: build com sysbuild, sem o Partition Manager depreciado.
- `CONFIG_RING_BUFFER=y` e a função `hal_uart_process()`.
- Contexto para assistentes de IA: `CLAUDE.md`, `AGENTS.md` e dez skills em `.claude/skills/` (`fw-build`, `fw-testes`, `fw-threads`, `fw-port-legacy`, `fw-hardware`, `fw-gps-sensores`, `fw-radio`, `fw-vue`, `docs-gnss`, `commit-gnss`).
- Documentação numerada em `docs/` (01 a 12, com índice), `README.md` e `legacy/README.md` (origem, licença CC BY-NC 4.0 e diferenças em relação ao upstream).
- `.gitattributes` (LF no repositório, CRLF nos `.bat`, `hardware/` e os dados de teste de `tools/TDD/` byte a byte) e `.editorconfig`.
- CI em `.github/workflows/ci.yml`, **desligado** (só `workflow_dispatch`): testes de host, documentação e build do firmware no container `sdk-nrf-toolchain:v3.3.0`. O `mermaid_check.py` aceita `PUPPETEER_CONFIG` para o Chrome do runner.
- Decisões do dono registradas em `docs/10`, `docs/07`, `docs/02` e nas skills: ANT+ e BLE juntos, pelo add-on `sdk-ant`, e board própria com MCU da Nordic.
- Alvo **nRF54LM20 DK** (`nrf54lm20dk/nrf54lm20a/cpuapp`), para desenvolver a placa própria: overlay com os nomes da aplicação em pinos do conector de expansão, `wdt31` ligado, configurações no ZMS (a NVM é RRAM) e a família do `nrfutil` escolhida pela `BOARD` no `fw.sh`, no `flash.bat` e no `recover.bat`. Compila com os 7 avisos conhecidos; não testado em placa.
- `docs/13-placa-nova.md`: proposta de hardware da placa própria, com pesquisa de mercado e de datasheets de 2026-09-18: nRF54LM20A no módulo Fanstel BM20C, display JDI LPM027M128C (MIP de 8 cores do tamanho do LS027; e-paper colorido descartado por levar de 11 a 20 s por quadro), GNSS u-blox MAX-F10S (L1 + L5) ou MAX-M10N no mesmo footprint, com antena linear na borda de cima como Garmin, COROS e Wahoo fazem (fotos internas do FCC), nPM1300 com o medidor MAX17262 e o carregador solar AEM10900 com painel ANYSOLAR, BMP585, LSM6DSV16X, LIS2MDL e OPT3001, orçamentos de pinos e de energia, riscos e próximos passos. Nada comprado nem testado; consumos e autonomia estimados.
- Segunda rodada de decisões: nRF54LM20A com esquemático próprio (GNSS, bateria e display melhores, painel solar pequeno na caixa), tela no formato do legacy (2,7", em retrato) e um commit por item verificado como regra do projeto; os links dos acordos do ANT+ entraram em `docs/07`.

### Alterado

- **GNSS da placa nova: u-blox MAX-F10S, de banda dupla L1 + L5, no lugar do MAX-M10N-10B** (decisão do dono em 2026-09-20). Ganha 1 m de CEP contra 1,5 m e o código do L5, dez vezes mais rápido, contra o multipercurso de prédio e de mata — o pior caso do legacy. Cabe sem mexer em nada: mesmo encapsulamento MAX de 10,1 × 9,7 × 2,5 mm, mesma pinagem, mesma antena de L1 e L5 e US$ 1,38 mais barato. Custa autonomia: o aparelho passa de cerca de 21 mW para cerca de 58 mW e de cerca de 310 h para cerca de 115 h sem sol, e o painel solar deixa de cobrir o consumo (passa a devolver de 23 a 46 min por hora de sol). O M10N-10B fica como alternativa econômica no mesmo footprint, e o firmware aceita as duas peças pelo devicetree.
  - Driver: compatível novo `u-blox,max-f10` no mesmo arquivo do M10 (`modules/gnss_drivers/drivers/gnss/`), com as chaves de `CFG-SIGNAL` de L5 e NavIC lidas da descrição de interface do F10 SPG 6.00 (UBX-23002975 R02, tabela 46). O grupo `CFG-PM` **não existe** no F10: `ublox_m10_set_power_mode()` devolve `-ENOTSUP` em vez de mandar uma chave que o receptor NAKaria, e `gnss_power_init()` passou a receber `has_leap`, de modo que a máquina nunca pede um modo que a peça não tem.
  - `ubx_m10_valset_many()`: um `UBX-CFG-VALSET` com várias chaves. Toda escrita em `CFG-SIGNAL` reinicia o subsistema GNSS e pede 0,5 s de espera depois do reconhecimento (seção 4.9.20), então as nove chaves de L1, L5 e NavIC saem num quadro só: um reinício em vez de nove.
  - Achado da ficha do F10S (UBXDOC-963802114-12732 R03, tabela 12): o RF_IN tem um único máximo absoluto de **0 dBm**, sem a exceção de +15 dBm fora da banda que a ficha do M10N-10B traz. Com o BLE e o ANT+ a +8 dBm na mesma placa, o isolamento entre as antenas passa a ser requisito de segurança do componente, e não só de desempenho: medir o S21 em 2,44 GHz antes de ligar o rádio na potência cheia.
  - Documentação: `docs/05`, `docs/06`, `docs/10`, `docs/12`, `docs/13`, `docs/14`, `docs/15`, `docs/16`, `docs/19`, `README.md` e `CLAUDE.md`. O `docs/13` ganhou um aviso de que o painel solar não sustenta o aparelho com o F10S. Testes: `test_ubx_m10` foi de 20 para 24 casos e `test_gnss_power` de 16 para 21; nada testado com receptor nenhum.
- `power_scheduler`: fica só a regra de tempo do legacy (`power_scheduler_run()` diz quando desligar); quem desliga é a máquina de sistema.
- O `main()` lê e valida as configurações no boot (`user_settings_enforce`), e o FTP e o peso passam a ser gravados pelos comandos da interface.
- `CLAUDE.md`, `docs/02`, `docs/03`, `docs/05` (reescrito), `docs/06`, `docs/08`, `docs/09`, `docs/10` (matriz, migração e defeitos), `docs/11`, `docs/12`, `docs/13`, `docs/16`, `README.md` e as skills seguem a arquitetura nova; o `CLAUDE.md` e a skill `fw-testes` registram que um `bash` chamado por script no Windows abre o WSL.
- Peças da placa nova depois da validação de compra (`docs/19-lista-de-compras.md`): a tela da lista é a Sharp LS027B7DH01A com o filme de luz frontal da Azumo, porque o JDI LPM027M128C não tem canal autorizado de compra (a placa continua aceitando o JDI no Hirose FH28-10S-0.5SH(05)); BMI270 e MMC5633NJL no lugar do LSM6DSV16X e do LIS2MDL, sem estoque; ESD761, NTC TDK NTCG103JF103FT1, JST GH de 6 vias e receptáculo Molex 2036150003 no lugar do ESD751, do NTC da Murata, do JST SH e do Amphenol; placa de 0,8 mm; luz da tela pela LDSW2 em 3,3 V; seção nova de ligações fixas dos CIs no `docs/14`; `tools/docs/case_drawing.py` com o BM20C de 16,2 mm, os sensores novos, o USB-C IPX8 sem tampa na traseira e a tela da lista na legenda.
- `docs/14-hardware-placa-nova.md`: o EXTCOMIN do JDI com a luz acesa vai a cerca de 120 Hz, e não 60 Hz: a ficha pede o COM perto de 60 Hz no modo transmissivo, e o COM é metade do EXTCOMIN (até 70 e 140 Hz). Sem luz, 1 Hz.
- `README.md`: o nRF54LM20A e o rádio BLE + ANT+ nos badges, a seção "Placa nova" com o desenho do aparelho, um diagrama e as escolhas principais, os comandos de build para o nRF54LM20 DK e com `ANT=1`, o aviso de que nada rodou em placa nem nos DKs, o estado do ANT e da placa nova, e a nota de que o material ANT+ fica fora do repositório.
- `docs/14-hardware-placa-nova.md` segue a avaliação: GNSS MAX-M10N-10B (com `UBX-RXM-PMREQ` antes de cortar o 1V8, rampa do V_IO, TIMEPULSE sem pull-down e RF_IN de no máximo 0 dBm), bloqueio da carga solar pelo VBUSOUT, indutor de 6,8 µH, 5 V opcional (REG710) para o Sharp, TXU0204 em VQFN-14 com o OE fixo, MAX17262 em WLP, SD NAND, USB-C IPX8, ESD751 no lugar do TPD1E10B06 (que conduziria com 9 a 20 V no VBUS), conector da bateria de 4 vias e 37 GPIO no protótipo. O `docs/13` e o desenho do aparelho passaram a apontar para essas escolhas, e o BMP585 deixou de ser descrito com gel, que a ficha não cita.
- `build.bat`, `build_ncs.bat`, `flash.bat`, `recover.bat` e `serial.bat` reescritos para o NCS v3.3.0: ambiente em `tools/fw/ncs_env.bat`, `west` rodando no drive do projeto, sysbuild, `-p auto`, gravação filtrada por J-Link (`--traits jlink` ou `NRF_SERIAL`), opção de preservar a partição de settings, porta serial por parâmetro.
- `.gitignore`: builds, caches do clangd, `__pycache__`, `node_modules` e `.claude/settings.local.json`.
- Imagens de `docs/` movidas para `docs/img/`; os quatro documentos de novembro de 2025 arquivados sem alteração em `docs/historico/2025-11/`.
- Tamanho: FLASH 294.796 B (−4,3 KB sem o driver QSPI do DK), RAM 118.080 B (+2,9 KB pelas pilhas maiores).
- A alimentação do WDT no boot, depois de um reset por software, usa o `watchdog0` do devicetree em vez do `NRF_WDT` fixo do nRF52: vale também para o `wdt31` do nRF54LM20; o binário do nRF52840 não mudou.
- O `crash_recovery` lê a causa do reset pela API `hwinfo` do Zephyr, que vale para o nRF52 (POWER) e o nRF54L (RESET), e decodifica o CFSR em qualquer núcleo ARMv7-M ou ARMv8-M Mainline (Cortex-M4 e M33), não só no M4. FLASH do nRF52840 +112 B.
- O código chega aos barramentos pelos aliases do devicetree (`gps-uart`, `sensor-i2c`, `lcd-spi`, `sdc-spi`), não pelas instâncias do nRF52 (`uart1`, `i2c0`, `spi1`, `spi2`): passo para compilar em outras placas; o binário do nRF52840 não mudou.
- O `CMakeLists.txt` não fixa mais a placa nem o overlay: a placa vem do `-b` (`BOARD` no `fw.sh` e no `build.bat`) e o overlay da V3 passou a `boards/nrf52840dk_nrf52840.overlay`, aplicado pelo nome; o overlay de teste de 2025-11 que ocupava esse nome e nunca entrava no build saiu. O binário do nRF52840 não mudou.
- `docs/07` e `docs/13`: a pilha ANT exige cristal de 32,768 kHz com no máximo ±50 ppm (os dois alvos do port já usam o cristal a 50 ppm; na placa nova, conferir o cristal do módulo), e a instalação do `sdk-ant` pela tag `v2.1.1` ou pelo índice de add-ons do VS Code.
- `docs/07` com o que o ANT+ Adopter Agreement (versão 20250103) e a página de downloads dizem de fato: nada dos perfis nem das ferramentas pode ser distribuído (regra nova no `CLAUDE.md`, porque o repositório é público), a marca ANT+ exige interoperabilidade testada por você, e os programas de membros e de certificação ANT+ terminaram em 2025-06-30.
- Repositório publicado no GitHub: `Guiimartinho/gnss-bike-computer`, público (o antigo `bike-computer-stravaV11`, renomeado), com `main` e `develop`; o histórico de 2025-11 do repositório antigo, sem ligação com o atual, ficou no ramo `archive/stravav11-2025-11`.
- Branches `main` (versões estáveis) e `develop` (trabalho) no lugar da `master`; regra registrada no `CLAUDE.md`, no `AGENTS.md` e na skill `commit-gnss`.
- Armadilhas de build registradas no `CLAUDE.md` e na skill `fw-build` (o build incremental guarda símbolos Kconfig antigos; caminhos de build longos demais) e menções aos builds antigos, já apagados, retiradas.

### Removido

- Substituídos pelos serviços e pelas APIs do Zephyr: o HAL próprio (`src/hal`), os drivers da V3 (`ls027`, `baro`, `fxos`, `stc3100`, `gps_mgmt`, `nmea_parser`, `gps_epo`, `neopixel`), a interface em paisagem (`src/vue`), a USB da pilha antiga (`src/usb`), os stubs do sistema de arquivos (`src/utils`), o `boucle`, o `model_lock`, o `zwift` (protocolo próprio, não o `$LOC` do legacy) e o `baro_drift` (duplicado), com os testes `test_gps_mgmt` e `test_nmea_parser` e os seus falsos. Os nós falsos de `gpio-keys` do overlay da V3 saíram, porque o subsistema de entrada os trataria como teclas; o nRF52840 DK ficou sem leitura de bateria (o STC3100 não tem driver no Zephyr).
- `include/rf/glasses.h`: stub sem uso que afirmava que o ANT+ não existia no Zephyr; o `include/rf/ant.h` deixou de ser stub.
- Template vazio de app da raiz (`CMakeLists.txt`, `prj.conf`, `src/main.c`), que vinha do commit inicial e não era o firmware.

## [2.0.0] - 2025-12-01

Port inicial para Zephyr, criado entre 2025-11-26 e 2025-11-28 e compilado pela última vez em 2025-12-01 com o NCS v3.1.0 (versão definida em `app_types.h`, sem tag).

### Adicionado

- `zephyr_app/` com HAL (GPIO, I2C, SPI, UART), drivers (LS027, BME280 e FXOS8700 sobre os drivers nativos, STC3100, GPS, parser NMEA, EPO, NeoPixel em stub), modelo (boucle, attitude, Kalman de 3 estados, locator, segmentos, listas de pontos, vetores, zonas de potência, suffer score, zonas RR, configurações em NVS, recuperação de falha, percurso, log no SD, Zwift), BLE (NUS, LNS, BAS, DIS e clientes HRS, CSC, FTMS e Komoot) e interface (9 páginas, menu, telas de rolo).
- Documentos de arquitetura, revisão MISRA, ambiente e gap analysis (25 e 26 de novembro de 2025), hoje em `docs/historico/`.
- Scripts `.bat` para o NCS v3.1.0.

## Legacy

O stravaV10 original (Vincent Gollé, 2015 a 2020, CC BY-NC 4.0) foi copiado para `legacy/`, `libraries/` e `tools/` em 2025-11-25 e o projeto da placa para `hardware/` em 2025-11-30. Essas pastas não são versionadas aqui como produto: são referência.
